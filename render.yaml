services:
  # ── Unified Java Spring Boot + React Application ───────────────────
  - type: web
    name: rajani-voice-analyser
    runtime: docker
    plan: free
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: RENDER_EXTERNAL_URL
        value: https://rajani-voice-analyser.onrender.com
